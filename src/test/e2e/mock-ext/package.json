{
  "name": "test-settings-mock",
  "publisher": "test",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.75.0"
  },
  "main": "./extension.js",
  "contributes": {
    "configuration": {
      "properties": {
        "foo": {
          "type": ["string", "number", "boolean", "array", "object"]
        },
        "bar": {
          "type": ["string", "number", "boolean", "array", "object"]
        },
        "shared": {
          "type": ["string", "number", "boolean", "array", "object"]
        }
      }
    }
  }
}
